import { Section } from '../../../common/model/SectionModel'

type SectionDetailsProps = {
    section: Section
}

export default function SectionDetails(props: SectionDetailsProps) {
    return (
        null
        //<p>{props.section.workingHours}</p>
    )
}